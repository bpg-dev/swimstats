{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>bpg/renovate-config",
    "github>bpg/renovate-config:automerge-github-actions",
    ":semanticCommitTypeAll(chore)"
  ],
  "packageRules": [
    {
      "description": "Bump the CI Go version together with the golang Docker image",
      "matchDepNames": ["go", "golang"],
      "matchUpdateTypes": ["major", "minor"],
      "groupName": "go toolchain"
    },
    {
      "description": "Bump the CI Node version together with the node Docker image",
      "matchDepNames": ["node"],
      "matchUpdateTypes": ["major", "minor"],
      "groupName": "node toolchain"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^\\.github/workflows/.*\\.ya?ml$/"
      ],
      "matchStrings": [
        "golangci-lint-action[\\s\\S]*?version:\\s*(?<currentValue>v\\d+\\.\\d+\\.\\d+)"
      ],
      "depNameTemplate": "golangci/golangci-lint",
      "datasourceTemplate": "github-releases"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/(^|/)Makefile$/"
      ],
      "matchStrings": [
        "GOLANGCI_LINT_VERSION\\s*\\?=\\s*(?<currentValue>v\\d+\\.\\d+\\.\\d+)"
      ],
      "depNameTemplate": "golangci/golangci-lint",
      "datasourceTemplate": "github-releases"
    }
  ]
}
